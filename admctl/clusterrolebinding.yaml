apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: devenv-control-rolebinding
subjects:
  - kind: ServiceAccount
    name: default
    namespace: devenv
roleRef:
  kind: ClusterRole
  name: devenv-control-role
  apiGroup: rbac.authorization.k8s.io
